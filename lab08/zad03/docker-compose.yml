version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:3000"
    depends_on:
      - redis
      - postgres
  redis:
    image: redis
  postgres:
    image: postgres
    environment:
      POSTGRES_USER: me
      POSTGRES_PASSWORD: my_password
